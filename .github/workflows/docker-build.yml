name: Build & Deploy Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker image
        run: |
          docker build -t ecg-arrhythmia-api:latest .

      - name: Run smoke test on image
        run: |
          docker run --rm ecg-arrhythmia-api:latest python -c "import src; print('Build OK')"

      # Future: Add steps to push to Docker registry and deploy to Render
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      #
      # - name: Push to Docker Hub
      #   run: |
      #     docker push <your-docker-repo>/ecg-arrhythmia-api:latest
